import React from "react";
import Calendar from "react-calendar";
import "./MyCalendar.css";

const MyCalendar = ({ onChange, value }) => (
  <div>
    {/* <Calendar onChange={onChange} value={value} hover={new Date()} /> */}
  </div>
);

export default MyCalendar;
